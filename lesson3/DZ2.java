package lesson3;

import java.util.Scanner;

public class DZ2 //Пользователь вводит с клавиатуры три целочисленных
// значения. На экран выводится информация можно ли из этих сторон
// построить треугольник.(необходимо вспомнить правило
// построения треугольника по трем сторонам).

{
    public static void main(String[] args) {


        Scanner scanner = new Scanner(System.in);
        int a = scanner.nextInt();
        int b = scanner.nextInt();
        int c = scanner.nextInt();
        { if  (a + b + c + a);
        System.out.println("можна вивести трикутник");}
        {
        if (a != b != c != a);
        System.out.println("не можливий трикутник");}
    }
}

















/* String scanerline = scanner.nextLine();
        String string = scanerline + scanerline.equals(a);
        if scanerline + scanerline.equals ();{
            System.out.println("Нельзя построить треугольник.");
        } else {
            System.out.println("Можно построить треугольник.");
        }
    }
}


/*String scanerline = scanner.nextLine();
        if scanerline + scanerline.equals (a, b, c) {
    System.out.println("Нельзя построить треугольник.");
} else {
        System.out.println("Можно построить треугольник.");
        }*/
